{
  "name": "@opencoredev/social-sdk-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build && node --experimental-sqlite --import tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "test": "bun test tests"
  },
  "dependencies": {
    "@opencoredev/social-sdk": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^6.0.3"
  }
}
